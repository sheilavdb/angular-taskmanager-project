import { HoverTooltipDirective } from './hover-tooltip.directive';

describe('HoverTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
