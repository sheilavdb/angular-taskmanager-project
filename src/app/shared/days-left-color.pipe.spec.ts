import { DaysLeftColorPipe } from './days-left-color.pipe';

describe('DaysLeftColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysLeftColorPipe();
    expect(pipe).toBeTruthy();
  });
});
